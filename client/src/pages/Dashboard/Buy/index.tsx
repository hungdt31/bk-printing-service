import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export const BuyPage = () => {
  return (
    <Card className="w-full m-3">
      <CardHeader>
        <CardTitle className="font-bold text-xl">Trang in</CardTitle>
        <CardDescription>Mua thêm trang in, thống kê lịch sử giao dịch</CardDescription>
      </CardHeader>
    </Card>
  )
}
